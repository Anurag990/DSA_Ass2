package DSA_Ass3;import java.util.Arrays;import java.util.Scanner;public class ques4 {    public static int FirstOccurrence(int []arr,int target)    {        int n = arr.length,low = 0,upper = n-1,answer = -1;        while(low<=upper)        {            int mid = (low + upper) / 2;            if(arr[mid] == target)            {                answer = mid;                upper = mid - 1;            }            else if(arr[mid] > target)            {                upper = mid - 1;            }            else if(arr[mid] < target)            {                low = mid + 1;            }        }        return answer;    }    public static void main(String[] args) {        //Given a sorted integer array containing duplicates, count occurrences of a given number.If the element        //not found in the array, report that as well.        Scanner sc = new Scanner(System.in);        int []arr = {2,5,5,5,6,6,8,9,9,9};        System.out.println("Given Array :" + Arrays.toString(arr));        System.out.println("Enter the element to count its occurrences in a given array ");        int target = sc.nextInt();        int idx = FirstOccurrence(arr, target);        if(idx == -1)        {            System.out.println("Element is not present in the array");            return;        }        int count=0;        while(idx < arr.length&&arr[idx]==target)        {            count++;            idx++;        }        System.out.println("Target "+target+" occurs "+count+" times ");    }}